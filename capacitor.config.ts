import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.netlify.luanarapp',
  appName: 'luanarapp',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
